spring:
  datasource:
    master:
      url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:travels}
    replica:
      url: jdbc:postgresql://${DB_RO_HOST:localhost}:${DB_RO_PORT:5433}/${DB_NAME:travels}